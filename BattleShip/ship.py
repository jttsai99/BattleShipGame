from .get_ship import Get_Ship
class Ship(object):
    def __init__(self,name,length,orientation,coordinates):
        self.name = name
        self.length = length
        self.orientation = orientation
        self.marker = self.name[0]
        pass
    def __str__(self) -> str:
        return(self.name,self.length)

    def ship_coordinates(coordinates, self.orientation, self.length):
        if orientation == "horizontal":
            self.name_coords=[coordinates]
            for i in range(len(self.length)):
                append.selcoordinates[1]+1

